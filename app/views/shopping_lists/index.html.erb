<h2>Shopping list</h2>

  <thead>

  <tr>
    <th>Name</th>
    <th class="text-center">Action</th>
  </tr>
  </thead>

  <% @shopping_lists.each do |shopping_list| %>
    <tr>
      <br>
      <br>
      <td><%= shopping_list.name %></td>
      <td>
      <%= link_to 'Show list', shopping_list_items_path(shopping_list) %>
      <%= link_to 'Destroy', shopping_list_path(shopping_list), method: :delete, data: { confirm: "Are you sure?" } %>
      </td>
    </tr>
  <% end %>
<br>
<br>

<%= link_to 'Create New List', new_shopping_list_path %>