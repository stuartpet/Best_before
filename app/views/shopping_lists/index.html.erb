<h1>Shopping lists</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
  </tr>

  <% @shopping_lists.each do |shopping_list| %>
    <tr>
      <td><%= shopping_list.name %></td>
      <td><%= link_to 'Show', shopping_list_path(shopping_list) %></td>
      <td><%= link_to 'Edit', edit_shopping_list_path(shopping_list) %></td>
    </tr>
  <% end %>
</table>