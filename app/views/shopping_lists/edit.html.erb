<h1>Edit Shopping List</h1>

<%= form_with(model: @shopping_lists, local: true) do |f| %>

  <% if @shopping_lists.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@shopping_lists.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @shopping_lists.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :list_name %><br>
    <%= f.text_field :name %>
  </p>

  <p>
    <%= f.submit %>
  </p>

<% end %>

<%= link_to 'Back',  shopping_lists_path %>